# TODO - Firebase integration (enrolment_service & roadmap_service)

- [ ] Inspect current enrolment + roadmap-related UI entry points.
- [ ] Update `LearnerProgramDetailsScreen` enrol button to also write to global `enrolments` via `EnrolmentService`.
- [ ] Add roadmap UI to `LearnerProgressScreen` (or appropriate screen) with clickable steps.
- [ ] Wire roadmap step completion to Firestore via `RoadmapService`.
- [ ] Ensure compile: run `flutter analyze` and fix any errors.
- [ ] Smoke test navigation + button clicks (manual).

