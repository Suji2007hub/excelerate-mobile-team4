name: excelerate_pathfinder

description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^3.2.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.1.0

  # Authentication providers
  google_sign_in: ^6.2.1

  # Open links, email, phone etc.
  url_launcher: ^6.3.0

  # Date formatting
  intl: ^0.19.0

  # Confetti animation
  confetti: ^0.7.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true
